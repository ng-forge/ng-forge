{
  "$comment": [
    "Golden corpus: reviewed expectations for what validates, independent of any one implementation.",
    "currentVerdict is what the live hand-written Zod schemas do NOW. It is asserted on every run,",
    "which is what makes Tier B a differential oracle rather than a record of history.",
    "v1Verdict is the reviewed target contract, asserted against the derived validator once it exists.",
    "A row may omit v1Verdict while a divergence is still an unreviewed candidate.",
    "Resolved divergences keep their history in `regression`, which is documentation and never asserted.",
    "Rows are added when a decision is made, not scraped from current behaviour, so the file records",
    "contract rather than whatever the schemas happened to do."
  ],
  "rows": [
    {
      "id": "core-container-bare",
      "description": "A container with required wrappers and a child field",
      "adapters": ["material", "bootstrap", "primeng", "ionic"],
      "config": {
        "fields": [
          {
            "key": "chrome",
            "type": "container",
            "wrappers": [],
            "fields": [{ "key": "email", "type": "input", "label": "Email" }]
          }
        ]
      },
      "currentVerdict": "valid",
      "v1Verdict": "valid",
      "rationale": "ContainerField reconciliation gate. Runtime support was proven across all four adapters before the schema changed, so accepting it is an evidence-backed contract expansion rather than an inference from the type registry.",
      "regression": {
        "observedBefore": "14610fc3c",
        "issue": "ContainerField was registered in FieldRegistryContainers and rendered by core BUILT_IN_FIELDS, but validation rejected it as an unknown field type"
      }
    },
    {
      "id": "core-container-missing-wrappers",
      "description": "A container with no wrappers property",
      "adapters": ["material", "bootstrap", "primeng", "ionic"],
      "config": {
        "fields": [
          {
            "key": "chrome",
            "type": "container",
            "fields": [{ "key": "email", "type": "input", "label": "Email" }]
          }
        ]
      },
      "currentVerdict": "invalid",
      "v1Verdict": "invalid",
      "rationale": "`wrappers` is required on ContainerField. A container without it is a group spelled differently, so accepting it would erase the distinction that justifies the type. This row exists to stop a future derivation from relaxing the property to optional."
    }
  ]
}
