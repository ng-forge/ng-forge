{
  "name": "dynamic-forms-material",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/dynamic-forms-material/src",
  "prefix": "df-mat",
  "projectType": "library",
  "implicitDependencies": ["dynamic-forms"],
  "release": {
    "version": {
      "manifestRootsToUpdate": ["{projectRoot}", "dist/{projectRoot}"],
      "currentVersionResolver": "git-tag",
      "fallbackCurrentVersionResolver": "disk"
    }
  },
  "tags": [],
  "targets": {
    "build": {
      "//": "Empty configurations propagate to compile via dependsOn (Nx 22). See scripts/strip-fesm-comments.mjs header for the full pipeline.",
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "inputs": ["production", "^production"],
      "dependsOn": ["compile"],
      "options": {
        "command": "node scripts/strip-fesm-comments.mjs dist/packages/dynamic-forms-material",
        "cwd": "{workspaceRoot}"
      },
      "configurations": {
        "production": {},
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "compile": {
      "executor": "@nx/angular:package",
      "outputs": ["{workspaceRoot}/dist/{projectRoot}"],
      "options": {
        "project": "packages/dynamic-forms-material/ng-package.json",
        "tsConfig": "packages/dynamic-forms-material/tsconfig.lib.json"
      },
      "configurations": {
        "production": {
          "tsConfig": "packages/dynamic-forms-material/tsconfig.lib.prod.json"
        },
        "development": {}
      },
      "defaultConfiguration": "production"
    },
    "nx-release-publish": {
      "options": {
        "packageRoot": "dist/{projectRoot}"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    },
    "type-test": {
      "executor": "@nx/vitest:test",
      "options": {
        "configFile": "packages/dynamic-forms-material/vite.config.typecheck.ts"
      }
    },
    "api-check": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "cache": true,
      "inputs": [
        "{workspaceRoot}/dist/{projectRoot}/types/**/*.d.ts",
        "{projectRoot}/api-extractor.json",
        "{workspaceRoot}/config/api-extractor.base.json",
        "{workspaceRoot}/etc/dynamic-forms-material.api.md"
      ],
      "options": {
        "command": "api-extractor run --config packages/dynamic-forms-material/api-extractor.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "api-update": {
      "executor": "nx:run-commands",
      "dependsOn": ["build"],
      "cache": false,
      "options": {
        "command": "api-extractor run --local --config packages/dynamic-forms-material/api-extractor.json",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
