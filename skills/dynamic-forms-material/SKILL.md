---
name: ng-forge-dynamic-forms-material
description: Angular Material specific field properties for @ng-forge/dynamic-forms. Use alongside the ng-forge-dynamic-forms skill when the project depends on @ng-forge/dynamic-forms-material.
---

<!-- Generated by scripts/generate-skills.ts. Do not edit by hand. -->

# ng-forge Dynamic Forms: Angular Material

Adapter specific `props` for `@ng-forge/dynamic-forms-material`.

This skill documents version **1.2.0**.

## Use with the core skill

This skill covers only what Angular Material adds. Field types, validation, conditional
logic and the authoring rules are the same for every adapter and live in the
`ng-forge-dynamic-forms` skill. Read that one first.

## Confirm the project uses this adapter

```bash
node -e "console.log(require('@ng-forge/dynamic-forms-material/package.json').version)"
```

If that fails, the project uses a different adapter and none of the properties
below apply. Two adapters cannot both provide the same field type: TypeScript
reports error 2717 when they try, so a project has one adapter per field type.

Fields are registered with `withMaterialFields()`.

## Validate against this adapter

```bash
npx --yes @ng-forge/dynamic-forms-cli@next "path/to/your.form.ts" --ui material
```

Passing the wrong `--ui` validates against a different adapter's properties, which
can accept a config this project will not render. `@next` is where the published
executable is; the `latest` tag is still a placeholder with no binary. Requires
Node 24 or newer.

## Reference

- [Adapter properties](references/props.md) — every `props` key Angular Material adds, per field type
