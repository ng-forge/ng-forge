import { DocPageComponent } from '../doc-page/doc-page.component';

/**
 * Examples route — renders DocPageComponent which detects slug='examples'
 * and shows ExamplesIndexComponent. This file exists so Analog doesn't
 * generate a ghost route reference for a deleted file.
 */
export default DocPageComponent;
